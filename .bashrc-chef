# .bashrc-chef

# source the bash login profile
#
# I couldn't get things to work properly invoking bash with both the --login
# and --rcfile options so instead just use --rcfile and source the login
# profile; it's not ideal but it works
#
[[ -f ~/.bash_profile ]] && . ~/.bash_profile

command -v chef >/dev/null 2>&1 && eval "$(chef shell-init bash)"
